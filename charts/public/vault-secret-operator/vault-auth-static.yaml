apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: static-auth
  namespace: sandbox
spec:
  method: kubernetes
  mount: kubernetes-auth-mount
  kubernetes:
    role: kvasmt-backend-env-role
    serviceAccount: kvasmt-api-sa
    audiences:
      - "https://kubernetes.default.svc"